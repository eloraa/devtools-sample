devtools-sample

Live Link
https://eloraa.github.io/devtools-sample/

Getting Started
To get you started you can simply clone the repository:

    git clone https://github.com/eloraa/devtools-sample

and install the dependencies

    pnpm install

Prerequisites
You need git to clone the repository. You can get git from
http://git-scm.com/

A number of node.js tools is necessary to initialize and test the project. You must have node.js and its package manager (npm) installed. You can get them from http://nodejs.org/. But I recommend using pnpm. You can get it from https://pnpm.io/installation/. The tools/modules used in this project are listed in package.json and include yarn, and vite.

Run the Application
Start development server:

    pnpm dev

Build for Production
Build the Application for production:

    pnpm build